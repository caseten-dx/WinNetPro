# WinNetPro — Status

## Current phase

**Phase 2 — Demo 002 (GUI) in flight: Electron foundation (Phase D) complete.** Demo-001 is closed-out and validated end-to-end (x64 .exe smoke 10/10 PASS via Mac→VM Prism emulation). Demo-002's CLI foundation landed in the prior session (Phases A+B+C); Phase D landed this session — Electron main + preload + IPC bridge over the same in-process application services the CLI uses. Empty (unstyled) Electron window stands up and successfully round-trips `listAdapters()` + `loadAllProfiles()` through the bridge. arch-reviewer PASS-WITH-NITS; the only Phase-D-bound nit (CSP `'unsafe-inline'`) was addressed in-session. Phases E–H ahead.

## Current focus

Active demo: **`demos/002-save-and-dry-run-gui/`** — Electron foundation complete; six designed frames pending in Phase E. 68/68 tests pass; arch-reviewer green on the Phase D diff. **Local Win VM dev-env is now usable for visual GUI verification** — UTM Guest Tools installed, display 1920×1080, bidirectional clipboard working; full session detail in the 2026-05-22 entry below.

What landed (cumulative across demos):

Demo-002 (active):
- `src/{domain,application,infrastructure,cli}/` copied verbatim from demo-001 then patched with three carry-forward fixes from demo-001's smoke test: ASCII-only `formatPlan()` output (em-dash → `--`), unknown-positional CLI argument rejection (in the run.ts dispatch layer; the inline parser in args.ts stays at 65 lines per ADR-0009), and a three-way `ipv4Verdict()` helper splitting `(no change)` / `(re-apply)` / `(changes)`. JSON shape of ChangePlan unchanged.
- `src/gui/` populated (Phase D): `main.ts` (Electron composition root mirroring `cli/run.ts`'s wiring — FakeProvider + JsonProfileRepository + systemClock; `frame: false` + `titleBarStyle: 'hidden'` for the Win11 custom title-bar design), `preload.ts` (typed `contextBridge.exposeInMainWorld('api', …)`), `ipc-handlers.ts` (pure factory; no Electron import — unit-testable), `api-types.ts` (shared `WindowApi` contract + `IPC_CHANNELS` list), `renderer/{index.html,index.ts}` (throwaway placeholder; tight CSP `default-src 'self'; script-src 'self'; style-src 'self'`). Security posture: `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`.
- 68 tests pass: 61 inherited + 7 new in Phase D (IPC handler factory contract). Build target `WinNetPro-CLI-demo-002.exe` via the parameterised `scripts/build-cli-sea.mjs`; `WinNetPro-GUI-demo-002.exe` comes in Phase G. Dev launch: `pnpm --filter demo-002 dev:gui` (bundles via shared `scripts/build-gui-dev.mjs` then runs `electron dist/gui/main.cjs`).
- Design hand-off committed at `docs/research/demo-002-gui-design-handoff/` (1.2 MB, 24 files: 6 frames × light/dark, `tokens.css` authoritative tokens — oklch + 4-px scale + Inter/JetBrains Mono with Segoe UI Variable/Cascadia Mono fallbacks, JSX component sketches, README with Windows 11 custom title-bar specs mapping 1:1 to the brief's §11 acceptance criteria). Brief at `docs/research/demo-002-gui-design.md` (self-corrected mid-session against five overclaims about NetSwitch).
- `demos/002-save-and-dry-run-gui/spec-overrides.md` files two design-driven overrides per ADR-0007 (drop All Profiles dropdown from gui-spec.md; defer runtime provider toggle to demo-003). Override 1 is a promotion candidate at demo-002's eventual closeout; override 2 stays through demo-003.

Inherited from demo-001 (still in service):
- CI: `.github/workflows/build-cli.yml` on `windows-latest`, Node 24, pnpm 9, Node SEA pipeline. Same workflow will be paralleled by `build-gui.yml` in demo-002 Phase G.
- Local dev surface: `scripts/bootstrap-windows.ps1` provisions a Win11 ARM VM; SSH from Mac via Tailscale (`ssh winvm`); `scripts/smoke-x64-cli.ps1` runs the 10-test smoke battery against any built .exe (parameterised; works for demo-002 once it ships).
- Demo-001 evidence at `demos/001-save-and-dry-run-cli/evidence/`. Demo-001 itself is immutable per ADR-0007.

Next work, in order:
1. **Phase E1 on-ramp** — land the two carried Phase-D arch-reviewer nits before any frame work: split `tsconfig.renderer.json` instead of widening the whole-demo tsconfig with DOM lib; move `declare global { interface Window { api: WindowApi } }` from `src/gui/renderer/index.ts` into `src/gui/renderer/global.d.ts`. Small surgical step; no frames yet.
2. **Phases E1–E3** — six GUI frames against the design hand-off, spread across roughly 3 sessions (shell + first-launch + DHCP → Static + validation → save modal + demo-003 preview + dark-variant polish). `tokens.css` lifted verbatim or near-verbatim into the renderer. Visual verification now possible on the local Win VM (1920×1080 desktop) in addition to Mac-side `pnpm --filter demo-002 dev:gui`.
3. **Phase F** — BDDs driven through both in-process application services AND headless Electron snapshots; evidence files include PNGs alongside Markdown.
4. **Phase G** — Build pipeline. Flesh out `scripts/build-gui-electron.mjs`, add `.github/workflows/build-gui.yml`, produce `WinNetPro-GUI-demo-002.exe`.
5. **Phase H** — Demo-002 `/closeout`. Candidate promotion of override 1 (drop All Profiles dropdown) into `docs/specs/gui-spec.md`.

## Carry-forward for demo-002 (in flight)

- **Mac↔VM ergonomic gaps (optional, not blocking).** Two ergonomic frictions while working inside the Win VM via the UTM window — both deferred this session as Karabiner-class fixes a future session can batch. (a) **Scroll direction:** Mac natural-scroll feels inverted inside Windows. Scroll Reverser 1.9 is installed via brew but has no per-app rules (global toggle only); the proper per-app tool is **Mos** (`brew install --cask mos`, add UTM to its exception list). (b) **⌘↔Ctrl remap:** typing ⌘C in the VM triggers Copilot (UTM maps Mac ⌘ → Windows key); muscle memory wants Ctrl+C. Proper fix is **Karabiner-Elements** with a complex modification keyed to UTM being frontmost. Neither blocks Phase E work — visual verification primarily happens on the Mac via `pnpm --filter demo-002 dev:gui`; the VM is for native Windows widget verification where these frictions are tolerable.
- **Phase E cleanups carried out of Phase D arch-review.** Two nits arch-reviewer flagged as "before Phase E commit": (a) split renderer into `tsconfig.renderer.json` instead of widening the whole-demo tsconfig with DOM lib (currently `lib: ["ES2023", "DOM"]` is set at the demo level, with a comment about the cost); (b) move `declare global { interface Window { api: WindowApi } }` from `src/gui/renderer/index.ts` into a `src/gui/renderer/global.d.ts` so additional renderer files inherit it without re-declaring. Both are inert today (Phase D's renderer is a single throwaway file) and become real once Phase E adds the six frames.
- **GUI implementation (Phases E–G).** Design hand-off in `docs/research/demo-002-gui-design-handoff/` is the binding visual spec. Phase E renders the six frames against tokens.css. Phase F adds BDD+screenshot evidence. Phase G builds the `.exe` via `electron-builder` and a parallel `.github/workflows/build-gui.yml`.
- **`spec-overrides.md` promotion at demo-002 closeout.** Override 1 (drop All Profiles dropdown from gui-spec.md) is a candidate for promotion once Phase E ships the rendered GUI and the audience can verify the visual decision. Override 2 (runtime provider toggle) is intentionally deferred to demo-003 and stays in spec-overrides.md until then.

## Carry-forward for demo-003

- **Matching warning gap (arch-reviewer nit from demo-001).** ADR-0006 requires `description` and `alias` match confidence to surface a warning, and `alias`-only requires confirmation even with `--yes`. Demo-001 (carried into demo-002 via copy-forward) implements the match priority and the ambiguous-refusal path, but does **not** emit the warning. Safe in demos 001 and 002 (both dry-run only — no real mutation). Must land before demo-003's real apply path inherits this code.
- **Real apply pipeline + snapshot/rollback + admin detection.** Demo-002's `FakeProvider` only exposes `listAdapters()`; `applyChangePlan`, `verify`, snapshot capture/rollback (ADR-0004 steps 9-12), and admin detection (safety-spec invariant 4) all land in demo-003.
- **Runtime provider toggle (override 2 from demo-002).** A runtime Fake/Windows toggle is designed-in-spirit (demo-003 preview frame `06-demo003-preview-*.png` shows it with an amber-keyed Safe Mode banner) but requires admin detection + confirmation modal + pre/post-state visualization to be safe. Demo-003 ships the gating; `docs/specs/gui-spec.md` updates at that demo's closeout.

## Tracking (not gating)

- **GH Actions runtime deprecation.** `actions/checkout@v4`, `setup-node@v4`, `upload-artifact@v4`, `pnpm/action-setup@v4` still run on Node.js 20 internally — forced upgrade by 2026-09-16. `windows-latest` redirects to `windows-2025-vs2026` by 2026-06-15. Track; not urgent for demo-002 GUI work.

## Open questions

- **SHA-pinning of GH Actions vs floating `@v4` tags.** Arch-reviewer flagged the floating-tag posture in `build-cli.yml`. Deferred per teaching-repo readability tradeoff; revisit if the repo grows beyond demo scope or if a supply-chain incident hits one of the upstream actions.
- **Audience-target architecture mismatch on local VM `.exe`.** The local Win11 ARM VM produces ARM64 `.exe` binaries; the audience demo machines are almost certainly x64. Local VM is for fast feedback only; the shippable `.exe` continues to come from GH Actions `windows-latest` (x64). No action needed unless a future demo needs ARM-targeted audience binaries.

## Recent sessions (rolling 5)

### 2026-05-22 — Win VM dev-env fixup: UTM Guest Tools install (display + clipboard)

- **Goal:** Execute the user-explicit first item from the prior session's carry-forward — UTM Guest Tools install on the Win11 ARM VM to unblock visual GUI verification for Phase E. End state: display ≥ 1200×800 (so the Electron BrowserWindow fits) and bidirectional Mac↔VM clipboard so source can be moved between editors. Pure infrastructure session; no repo code changes.
- **Source:** User invoked `/startup`; picked "Walk me through VM fixup" from the next-step prompt. STATUS.md's prior Carry-forward documented the full click-path; this session executed it and verified each step via SSH probes.
- **Done:**
  - **Pre-state probe (via SSH-piped `vm-probe.ps1`, throwaway):** confirmed no SPICE/vdagent/virtio-gpu services running, no `C:\Program Files (x86)\UTM Guest Tools\` install dir, `.NET PrimaryScreen.Bounds` reporting 1024×768 (prior STATUS.md said 800×600; reality was 1024×768 — either way under the 1200×800 threshold). Probe scripts cleaned up at session end (both Mac `/tmp/` and VM `C:\Users\mgilmore\`).
  - **Mac-side UTM config (user-driven, with screenshots):** VM shut down cleanly via `ssh winvm 'shutdown /s /t 0'`; in UTM Settings → Sharing tab enabled **Clipboard Sharing**; verified Display tab already on `virtio-ramfb-gl (GPU Supported)` with **Resize display to window size automatically** and **Retina Mode** checked (all three are SPICE-dependent — UTM warns "Requires SPICE guest agent tools to be installed" right in the panel). Saved.
  - **Guest Tools install (inside VM, as admin):** booted VM, used the UTM toolbar CD-icon → **Install Windows Guest Tools…** to mount UTM's bundled Guest Tools ISO, ran `spice-guest-tools-xxx.exe` as admin, accepted defaults, rebooted.
  - **Post-install SSH transient:** first re-probe after reboot timed out — Tailscale's view of the VM showed `offline, last seen 14m ago`. Tailscale's Windows service didn't auto-reconnect after the post-install reboot (likely because the new virtio-gpu driver re-enumerated the network stack and Tailscale missed the boat). Recovered on its own after ~5 minutes without intervention.
  - **Post-install probe — install confirmed:** `vdagent.exe` running as PID 3792 from `C:\Program Files (x86)\UTM Guest Tools\ARM64\vdagent.exe` (UTM-vendored path, **not** the stock Red Hat `C:\Program Files\Red Hat\Spice\` layout the standalone spice-space.org installer uses — my initial probe checked the wrong path and reported `absent` until I broadened the check). `QEMU-GA` + `spice-webdavd` services both `Running` with `StartType=Automatic`. Video controller now reports `Red Hat VirtIO GPU DOD controller`, driver 22.7.38.43.
  - **Display verified at 1920×1080 — with a DOD-driver quirk worth recording:** user resized the UTM window + manually picked 1920×1080 in Settings → Display. The Advanced Display panel revealed: **Desktop mode 1920×1080** but **Active signal mode stays at 1024×768**. This is a property of the Red Hat VirtIO **DOD** (Display-Only Driver) — it renders the desktop at the selected resolution and downsamples into a fixed 1024×768 signal. Visually acceptable for the 1200×800 Electron window. Also explains why `.NET PrimaryScreen.Bounds` continues to return 1024×768 even post-fix: it reads the signal mode, not the desktop mode. Don't trust that .NET API for "is the desktop big enough" on this driver — read `Get-CimInstance Win32_VideoController.CurrentHorizontalResolution` or the user's screenshot instead.
  - **Clipboard verified both directions:** Mac → VM (`mac-to-vm-clipboard-test-2026` via ⌘C → Win+V or Ctrl+V in Notepad ✓); VM → Mac (`this is a test` via **Ctrl+C** → ⌘V on Mac ✓). One sharp edge worth knowing: **⌘C inside the VM does NOT work** — UTM maps Mac ⌘ to the Windows key, which on Win11 triggers Copilot. Must use Ctrl+C inside the VM.
  - **Scroll direction deferred:** installed Scroll Reverser 1.9 via `brew install --cask scroll-reverser` and walked through Accessibility-permission grant. On discovering Scroll Reverser has **no per-app rules** (only a global toggle from the menu bar), user opted to skip rather than retrain muscle memory across all Mac apps. Scroll Reverser stays installed but unused (can `brew uninstall --cask scroll-reverser` any time). Documented in the new "Mac↔VM ergonomic gaps" Carry-forward bullet: the proper per-app tool is **Mos** (scroll) and **Karabiner-Elements** (⌘→Ctrl); both deferred as a future ergonomic-batch session.
- **Next:**
  - **Phase E1 on-ramp** (next session): land the two carried Phase-D arch-reviewer nits before any frame work — split `tsconfig.renderer.json` instead of widening the whole-demo tsconfig with DOM lib, and move the `window.api` global declaration into `src/gui/renderer/global.d.ts`.
  - Then **Phase E1 frames**: shell + first-launch against `tokens.css` from the design hand-off. Visual verification now possible both on Mac (via `pnpm --filter demo-002 dev:gui`) and inside the Win VM at 1920×1080 (for native Windows widget rendering).
  - **Mac↔VM ergonomic batch** (optional, future session): install Mos + Karabiner-Elements + scope a UTM-frontmost-only profile. Not gating any work.

### 2026-05-21 — Demo-002 Phase D: Electron foundation + IPC bridge

- **Goal:** Land Phase D of demo-002 — the Electron foundation. Per STATUS.md's prior "Next work" item 1: "main + preload + IPC bridge from the renderer to the in-process application services. Adds `electron` + `electron-builder` devDeps. Empty Electron window stands up; no frames yet." End-state proof: `pnpm --filter demo-002 dev:gui` opens a window that calls real application services through the bridge.
- **Source:** User invoked `/startup` → confirmed Phase D as the next move. Surfaced two scoping forks: (1) custom Win11 title bar (`frame: false`) vs native frame; (2) renderer placeholder as functional IPC smoke vs static HTML. User asked for tradeoff analysis; chose "Fork 1" interpreted as "the first option in each fork" — custom title bar + functional IPC smoke (also the recommended path).
- **Done:**
  - **Shared dev bundler (commit pending):** `scripts/build-gui-dev.mjs` — esbuild bundler for any demo's GUI sources. Three outputs: `dist/gui/main.cjs` + `dist/gui/preload.cjs` (Node target, CJS, electron external), `dist/gui/renderer/index.js` (browser target, IIFE), plus a copy of `renderer/index.html`. Parameterised by `<NNN> <slug>` like the existing CLI SEA script; build time on Mac ≈ 0.3s warm. Phase G's `scripts/build-gui-electron.mjs` (still defensive stub) will call this first then run `electron-builder`.
  - **Composition root, mirroring CLI:** `src/gui/main.ts` constructs `FakeProvider` + `JsonProfileRepository` + `systemClock` exactly like `cli/run.ts` (CLAUDE.md invariant 6); calls `createIpcHandlers(...)`; for each channel in `IPC_CHANNELS` registers `ipcMain.handle`. BrowserWindow: 1200×800 (min 1000×700), `frame: false`, `titleBarStyle: 'hidden'` (Win11 custom title bar per design hand-off), `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`. Loads `renderer/index.html` via `loadFile`. macOS lifecycle handled (window-all-closed quits on non-darwin; activate re-creates window on Mac).
  - **Typed bridge surface:** `src/gui/api-types.ts` defines `WindowApi` (5 methods: `listAdapters`, `loadAllProfiles`, `validateProfileSaveInput`, `saveProfile`, `buildDryRunPlan`) and the `IPC_CHANNELS` tuple as a `satisfies readonly (keyof WindowApi)[]` so any drift is a compile error. Both `preload.ts` and `ipc-handlers.ts` implement `WindowApi`; TypeScript verifies they stay in lockstep.
  - **Preload (typed contextBridge):** `src/gui/preload.ts` — one-page file; declares `const api: WindowApi = { ... }` with each method routed via `ipcRenderer.invoke(channel, ...args)`; exposes via `contextBridge.exposeInMainWorld('api', api)`. No `nodeIntegration` escape hatches.
  - **Pure IPC handler factory:** `src/gui/ipc-handlers.ts` — no Electron import, takes `{ provider, repository, clock }`, returns a `WindowApi` whose methods route to `saveProfile`/`buildDryRunPlan`/`validateProfileSaveInput` and the provider's `listAdapters` / repository's `loadAll`. Pure factory means unit tests need no Electron runtime.
  - **Renderer placeholder (functional smoke):** `src/gui/renderer/{index.html,index.ts}`. HTML carries a tight CSP (`default-src 'self'; script-src 'self'; style-src 'self'`) — no inline styles, so the placeholder renders unstyled with Times-default. Renderer calls `Promise.all([listAdapters, loadAllProfiles])` and renders results with HTML-escaped values. Phase E rips this entirely and renders the six designed frames against tokens.css.
  - **DevDeps + scripts:** added `electron ^32.0.0` + `electron-builder ^25.0.0`. New scripts: `bundle:gui` (calls the shared dev bundler), `dev:gui` (bundle then launch). `pnpm install` took ~17s including electron postinstall (downloads the Electron binary).
  - **tsconfig DOM lib:** widened demo-002's `lib` to `["ES2023", "DOM"]` so the renderer compiles. Documented cost: backend code could now reference `document`/`window` without a type error. Mitigated by esbuild's platform/target catching it at bundle time and by Phase E splitting into per-subsystem tsconfig (arch-reviewer nit, deferred to Phase E).
  - **Tests:** new `tests/unit/ipc-handlers.test.ts` — 7 tests against the factory: every channel exposes a function; `listAdapters` round-trips through `FakeProvider`; `loadAllProfiles` returns `[]` from a fresh dir; `validateProfileSaveInput` surfaces empty-name as an error without persisting; `saveProfile` persists and the next `loadAllProfiles` sees it; `buildDryRunPlan` returns `unknown-profile` for an unsaved name and a real `ChangePlan` for a saved one. **68/68 demo tests pass** (61 inherited + 7 new); typecheck clean.
  - **arch-reviewer: PASS-WITH-NITS.** Verified CLAUDE.md invariants 1 (fake provider default, no `--real` branch in main.ts), 6 (zero networking logic in src/gui/; composition root mirrors CLI exactly), ADR-0007 (demo-001 untouched; shared script at repo root is permitted). Electron security posture clean (contextIsolation/nodeIntegration/sandbox flags as spec'd). Four nits: (1) CSP `'unsafe-inline'` in `style-src` — **addressed in-session** by removing the inline `<style>` block entirely and tightening to `'self'`. (2) split `tsconfig.renderer.json` before Phase E. (3) move `window.api` global declaration to `src/gui/renderer/global.d.ts` before Phase E. (4) `electron-builder` pull-forward — STATUS.md explicitly mandated this in Phase D; defended.
  - **Empirical Mac launch verification:** user ran `pnpm --filter demo-002 dev:gui`, captured a screenshot — frameless 1200×800 window with the H1, three adapter rows (`ad-01 — Ethernet`, `ad-02 — Ethernet 5`, `ad-03 — Wi-Fi`), and `(no profiles)`. Bridge round-trip confirmed end-to-end.
- **Side excursion — Win VM dev-env:** user flagged that the Win11 ARM VM is stuck at 800×600 and lacks Mac↔VM clipboard. Researched current state (UTM 5.0.0, Jan 2026): the standalone spice-space.org installer is x86/x64 only, but UTM bundles a Guest Tools ISO that includes ARM64 builds of virtio drivers + SPICE vdagent. Path documented in Carry-forward; not actioned this session (UTM-GUI work, not scriptable from SSH).
- **Next:** First thing next session — Win VM dev-env fixup (UTM Guest Tools install, display ≥1280×800, clipboard, reverse scroll direction). Then Phases E1–E3: six designed frames against `tokens.css` from the hand-off, three sessions. Phase E begins with the two carried arch-reviewer nits (renderer tsconfig split + global.d.ts).

### 2026-05-20 — x64 smoke validation, demo-002 scaffold, CLI foundation (Phase A+B+C)

- **Goal:** Two arcs in one session. (1) Verify the demo-001 x64 `.exe` behaves correctly on real Windows by exercising it through Prism emulation on the local Win11 ARM VM, Mac-driven via SSH (no UTM-window context-switching). (2) Scope demo-002 (the first GUI demo), receive a Claude Design web hand-off, and lay the CLI foundation by copy-forward + the three CLI carry-forward fixes the smoke test surfaces.
- **Source:** User invoked `/startup`, picked option 1 ("GUI over demo-001 surface") for demo-002 scope, "in-process services + screenshots" for BDD style, "docs/research/" for design-brief placement, "land all three" for the CLI carry-forwards, "Phase A+B+C in one session" per the Opus 4.7 + 1M-context calibration in-session, arch-reviewer at the end. User dismissed the per-phase-vs-end arch-reviewer question; I defaulted to "end of A+B+C."
- **Done:**
  - **x64 smoke validation (commit `6baf2f6`):** `scripts/smoke-x64-cli.ps1` (new, 262 lines) — reusable Windows-side harness exercising all four exit-code lanes (0/1/2/3) and the non-TTY auto-dry-run safety default. Uses `Start-Process` with file redirects (mirroring `build-cli.yml`) because pipeline `& $exe 2>&1` interacts badly with pwsh defaults. Ran end-to-end from this Mac terminal: `gh run download` → `scp` to VM → `ssh winvm 'powershell …'` → 10/10 PASS. Three findings surfaced (all the kinds the smoke test was designed to catch, none safety-critical in a dry-run-only demo):
    - (1) U+2014 EM DASH on `dns:`/`ipv6:` lines mojibakes on default Windows consoles. The .exe writes valid UTF-8 — verified at byte level `E2 80 94` — but PS 5.1 + cmd.exe default to OEM/ANSI and rendered the bytes as `�?"`.
    - (2) CLI arg parser silently swallows unknown positional args. Surfaced via PS 5.1's `Start-Process -ArgumentList` not quoting `--description "lab bench"`; the .exe received `--description lab bench` as three tokens, took `lab` as the value, dropped `bench`.
    - (3) `dhcp -> dhcp (changes)` reads contradictorily — `willChange=true` currently means "mutation API will be called", not "end-state differs."
  - **Demo-002 scoping + design (commit `184d90e`):** User showed Codex's parallel implementation (NetSwitch) and challenged "do better." Audited screenshots, wrote `docs/research/demo-002-gui-design.md` — a Claude-Design-web-ready brief. Mid-session self-correction after honest re-look at NetSwitch: walked back five overclaims (progressive-disclosure → disabled-fields is defensible; grey-on-grey → "loses visual-hierarchy contest in slot 9 of 10"; refresh-staring → NetSwitch's progress-bar+step+elapsed IS good UX; dropdown duplication is gui-spec.md's call not NetSwitch's; added "preserve" list for tables / all-verbs-row / streaming-feedback / runtime-toggle). User took the corrected brief to Claude Design web, returned `~/Downloads/WinNetPro.zip` (1.2 MB, 24 files). Extracted to `docs/research/demo-002-gui-design-handoff/`: 6 frames × light/dark, `tokens.css` (authoritative — oklch + 4-px scale + Inter/JetBrains Mono with Segoe UI Variable/Cascadia Mono fallbacks), JSX components, README with Windows 11 custom title-bar specs mapping 1:1 to the brief's §11 acceptance criteria. Design is structurally better than NetSwitch on every axis the brief flagged plus several it didn't ask about (live recompute timestamp, match-confidence rendering on the change plan, computed-mask hint on prefix-length, App Alias "Never written to Windows" microcopy, demo-002 chip as scope signal).
  - **Demo-002 scaffold (commit `3dd14f1`):** `/create-demo 002 "save-and-dry-run-gui"` produced the pnpm workspace member (`package.json` mirroring demo-001 + Electron-deferred-to-implementation, `tsconfig.json` adding `.tsx`, `fixtures/adapters.json` copied from demo-001, `src/{domain,application,infrastructure,cli,gui}/` subdirs, README pointing at brief + hand-off, `bdd-additions/` empty). `pnpm install` recognised the workspace; `typecheck` correctly failed with `TS18003` confirming wiring.
  - **Phase A — copy-forward (commit `2a5119a`):** `cp -R` of demo-001's `src/` and `tests/` verbatim. 50/50 tests pass.
  - **Copy-forward cleanup (commit `f4d9f4e`):** updated eight `demo-001` references in copied code — five audience-visible (stderr messages in `run.ts` + `profiles-apply.ts`, evidence header in `_runner.ts` made path-derived via `basename(resolve(HERE, '../..'))` so the bug doesn't propagate forward) and three documentary comments. Demo-001's runner stays untouched per ADR-0007 immutability spirit.
  - **Phase B fix 1 (commit `79d6929`):** swapped U+2014 EM DASH for ASCII `--` in `formatPlan()`. Exported `formatPlan` (with a comment justifying the surface change) so the test can guard ASCII-only output via a codepoint scan. 3 new tests; 53/53 pass.
  - **Phase B fix 2 (commit `2d15599`):** added unknown-positional-argument rejection in `run.ts:58-61` at the dispatch layer — NOT in `args.ts`. ADR-0009's inline-parser constraint is preserved; `args.ts` stays at exactly 65 lines as a pure token classifier. 4 new tests; 57/57 pass.
  - **Phase B fix 3 (commit `b201089`):** new `ipv4Verdict()` helper renders `(no change)` / `(re-apply)` / `(changes)` based on `willChange` × mode-equivalence. ChangePlan JSON shape unchanged — this is presentational disambiguation only. 4 new tests; 61/61 pass.
  - **Phase C — spec-overrides (commit `56a6593`):** `demos/002-save-and-dry-run-gui/spec-overrides.md` files two overrides per ADR-0007: drop "All profiles" dropdown from gui-spec.md (promotion candidate at demo-002 closeout post-Phase-E); defer runtime provider toggle to demo-003.
  - **arch-reviewer: PASS** on the cumulative `3dd14f1^..HEAD` diff. Seven invariants verified individually. ADR-0009 specifically confirmed (`args.ts` at 65 lines unchanged; rejection lives in `run.ts` dispatch layer). Zero scope-creep flags. Demo-001 confirmed untouched; canonical `docs/` confirmed untouched.
- **Next:** Phase D — Electron foundation (main + preload + IPC bridge from renderer to in-process application services). Fresh session; adds `electron` + `electron-builder` devDeps; empty Electron window stands up; no frames yet. Phases E1–E3 (six GUI frames against the hand-off, ~3 sessions), F (BDDs + headless Electron screenshots), G (`scripts/build-gui-electron.mjs` + `.github/workflows/build-gui.yml` + `WinNetPro-GUI-demo-002.exe`), H (`/closeout` + override 1 promotion).

### 2026-05-20 — Local Windows ARM VM as a Mac-driven development surface

- **Goal:** Stand up a local Windows VM that this Mac session can fully drive over SSH — clone, install, test, build, smoke — without ever clicking into the VM's console. The CI `.exe` pipeline from the previous session covers the audience-shippable artifact; a local VM closes the inner feedback loop (no GH Actions roundtrip for "does this still work on Windows?" checks) and lays groundwork for demo-002's GUI development where iterating on Electron locally is essential.
- **Source:** User asked the broader "Windows-from-Mac" question. Surveyed VPS options (Vultr, Azure B-series, Hetzner with Win11 ISO + TPM bypass) and one-click Windows Server paths (AWS Lightsail, Windows 365 Cloud PC). User chose **local UTM + Windows 11 ARM** on Apple Silicon: free, no monthly bill, near-native performance via QEMU/hvf. The Apple-Silicon-vs-x64 gotcha is documented (`.exe` produced inside the VM is ARM64 → audience binaries still come from GH Actions). For Mac↔VM networking, user hit UTM's removed port-forwarding UI in current versions, pivoted to Tailscale.
- **Done:**
  - **`scripts/bootstrap-windows.ps1`** (new) — single PowerShell file that turns a fresh Win11 ARM VM into a ready-to-SSH dev host with one line: `irm <raw-url> | iex`. Idempotent. Seven numbered phases: OpenSSH Server install + firewall, winget toolchain (Git, PowerShell 7, Node.js), Tailscale via direct MSI from `pkgs.tailscale.com/stable/` (winget's `tailscale.tailscale` manifest is amd64-only), `pnpm@9` via npm, `OpenSSH DefaultShell` → `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`, machine-wide `administrators_authorized_keys` plumbing, `tailscale up` for browser SSO. Iterated through six failure modes before green:
    1. `pnpm/action-setup@v4` Multiple-Versions conflict on first CI run — fixed by dropping `version: 9` in favor of the root `packageManager` (last-session work, but the same pattern).
    2. Bootstrap pass 1: `msstore` certificate `0x8a15005e` → winget refused to disambiguate between sources when packages exist in both. Fixed with `--source winget` on every install and `$LASTEXITCODE` checks after each.
    3. Bootstrap pass 2: `tailscale.tailscale` exit `-1978335212` (`NO_APPLICATIONS_FOUND`) — winget manifest has no ARM64 entry. Replaced with direct-MSI install: parse latest versioned filename from the `pkgs.tailscale.com/stable/` HTML index, `msiexec /quiet /norestart /i`.
    4. Bootstrap pass 3: `InvokeMethodOnNull` at `[System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture.ToString()` — that .NET Core API returns `$null` under Windows PowerShell 5.1 (the OOBE default shell). Replaced with `$env:PROCESSOR_ARCHITECTURE` which has been stable since NT 3.1.
    5. Bootstrap pass 4: `npm.ps1 cannot be loaded because running scripts is disabled` — Windows PowerShell 5.1's default ExecutionPolicy is `Restricted`. Added `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force` at the top of the script.
    6. Bootstrap pass 5: `DefaultShell` registry value never wrote because `Test-Path 'C:\Program Files\PowerShell\7\pwsh.exe'` returned false — winget Microsoft.PowerShell on ARM64 Windows installs to a per-user WindowsApps App Execution Alias path that varies by username. Switched DefaultShell target to the fixed-path Windows PowerShell 5.1, and added `Set-ExecutionPolicy -Scope LocalMachine -ExecutionPolicy RemoteSigned -Force` so SSH sessions (not just the bootstrap process) can load .ps1 files.
  - **Mac-side Tailscale.** Installed via brew cask; GUI "Add Account" flow failed with "Unable to add new user" due to a sandboxed-keychain quirk; recovered via CLI `tailscale login`. macOS Sequoia required approving the Tailscale system extension (System Settings → General → Login Items & Extensions → Network Extensions). Mac at `100.83.43.52`.
  - **SSH from Mac to VM.** `~/.ssh/winnetpro_vm` keypair generated previously; `~/.ssh/config` `Host winvm` block added (HostName 100.71.232.112, User mgilmore, IdentityFile, IdentitiesOnly). Mac pubkey appended to VM's `C:\ProgramData\ssh\administrators_authorized_keys` (machine-wide for admin accounts, so any admin SSH user resolves to the same key). Smoke: `ssh winvm 'whoami; node --version; pnpm --version; git --version; tailscale ip -4'` returns clean output, key-based, no password.
  - **User migration: `Michael Gilmore` → `mgilmore`.** OOBE-created the first account as "Michael Gilmore" with a space in the username; rather than rename the profile folder (risky — Windows really doesn't want you to do that), created a new `mgilmore` local admin via `New-LocalUser`, added to Administrators group, interactive first-login at the UTM console once to provision the profile folder, then resumed SSH-driven work as `mgilmore`. Repo migrated from `C:\src\WinNetPro` to `C:\Users\mgilmore\Developer\WinNetPro` (mirrors Mac `~/Developer/WinNetPro`). Old `C:\src\` tree removed cleanly. The shared admin SSH keys file meant no key re-upload needed.
  - **End-to-end Windows-ARM validation** (all from this Mac terminal, no UTM clicks): `git clone` → `pnpm install --frozen-lockfile` (3.4 s) → `pnpm --filter demo-001 check` (typecheck OK, **50/50 tests** in 1.7 s) → `pnpm --filter demo-001 build:cli` (**26.5 s** — under the 30 s CLAUDE.md budget, ARM64 `.exe` produced) → smoke 1 no-args → exit 2 + `Usage:` ✓ → smoke 2 `profiles save --json` against bundled fixture → exit 0 + valid JSON profile `mgilmore-smoke` ✓.
- **Next:**
  - User smoke-tests the GH-Actions-built x64 `WinNetPro-CLI-demo-001.exe` on a Windows laptop. The ARM VM has already proven cross-platform code correctness; this remaining test exists to catch x64-vs-ARM behavioral differences (extremely unlikely for our domain code, possible for edge-case Node SEA path resolution).
  - Demo-002 scoping conversation: real apply pipeline + snapshot/rollback (ADR-0004 steps 9-12) + admin detection + the ADR-0006 description/alias warning emission carry-forward + the Electron GUI + parallel `build-gui.yml` workflow. With the local VM now in place, GUI iteration can happen via `pnpm electron .` on Mac for the renderer, and Windows-ARM smoke tests via the VM for native widget verification before pushing to CI.

### 2026-05-19 — Demo 001 anchor `.exe` shipping via GH Actions windows-latest

- **Goal:** Produce the demo-001 anchor `.exe` artifact via CI so the audience can download a working binary, and defer the persistent Windows-host decision until demo-002 actually needs real adapter mutation. This unblocks STATUS.md's "Next work" item 1 from the prior session.
- **Source:** User asked the broader question of how to do Windows development from a Mac. We surveyed the field (Azure Lightsail / Vultr / Windows 365 / Hetzner-DIY) and the Apple-Silicon-vs-x64 gotcha for Node SEA binaries. User concluded: GH Actions `windows-latest` for the `.exe` build today, manual Windows-laptop testing for now, persistent Windows host deferred until demo-002 GUI/real-apply needs it.
- **Done:**
  - `scripts/build-cli-sea.mjs` — replaced the defensive stub with the full Node SEA pipeline: esbuild bundle (`target: node24`, CJS, single-file) → `node --experimental-sea-config` → copy `process.execPath` → `postject.inject(...)`. On macOS, strips and re-signs the binary around the inject step so it runs locally for sanity checks. Resolves `esbuild` and `postject` from the demo's own `package.json` via `createRequire` (preserves ADR-0007's "shared scripts, per-demo deps" model). Local Mac build measured at 9.1 s; under the 30 s CLAUDE.md budget.
  - `demos/001-save-and-dry-run-cli/src/cli/index.ts` — dropped the top-level `await` so esbuild can bundle the CLI entry to CJS for SEA. The `run()` surface in `run.ts` is unchanged; tests do not touch `index.ts`; all 50 tests still pass after the refactor. Inline comment notes the constraint.
  - `demos/001-save-and-dry-run-cli/package.json` — added `esbuild ^0.24.0` and `postject ^1.0.0-alpha.6` devDeps.
  - `.github/workflows/build-cli.yml` (new) — `runs-on: windows-latest`, Node 24, pnpm 9 (read from root `packageManager`), `timeout-minutes: 10`, explicit `permissions: contents: read`. Steps: checkout → setup pnpm → setup Node → install (frozen lockfile) → typecheck (demo-001) → test (demo-001) → build:cli → verify `.exe` exists → smoke test (no-args → exit 2 + `Usage:` in stderr) → smoke test (`profiles save --json` against bundled fixture, temp `--config-dir` in `$RUNNER_TEMP`, expects exit 0 + valid JSON with `name: "ci-smoke"`) → upload artifact (`retention-days: 30`, `if-no-files-found: error`). Triggers: push-to-main with path filter, plus `workflow_dispatch`.
  - **arch-reviewer:** PASS-WITH-NITS. Three of four addressed inline before commit: esbuild target aligned to `node24`, end-to-end fixture-backed smoke step added, explicit `permissions:` block added. Fourth (floating `@v4` action tags vs SHA pins) deferred intentionally for live-readability in a teaching repo; tracked in Open Questions.
  - **CI shakedown:** four pushes to first green. Run 26123033477 failed on `pnpm/action-setup@v4` because both `version: 9` (workflow input) and `pnpm@9.0.0` (root `packageManager`) were declared — resolved by dropping the workflow input so the `packageManager` field is the single source of truth. Runs 26123138382 and 26124183434 reached the no-args smoke test but failed even though the `.exe` exited 2 with `Usage:` in output; the pipeline-redirect form `& $exe 2>&1 | Out-String` interacts with GH Actions' pwsh defaults (`$ErrorActionPreference = 'Stop'` + `$PSNativeCommandUseErrorActionPreference = $true`) in a way that taints the script's final exit state even after assertions complete. Rewrote both smoke tests with `Start-Process` + file redirects (no pipeline, no `$LASTEXITCODE` coupling, explicit `exit 0`) — run 26125202534 green in 59 s.
  - **Artifact:** `WinNetPro-CLI-demo-001` at https://github.com/caseten-dx/WinNetPro/actions/runs/26125202534 (~33.6 MB compressed zip, ~108 MB `.exe`), retained until 2026-06-18.
- **Next:**
  - User smoke-tests the `.exe` on a Windows laptop and reports back. Minimum surface: no-args → usage, a real `profiles save` against the bundled fixtures, a `profiles apply --dry-run` against the saved profile. Anything Windows-specific (path separators, line endings, fixture resolution) is feedback we want before demo-002.
  - Demo-002 scoping conversation: real apply pipeline, snapshot/rollback (ADR-0004 steps 9-12), admin detection, the ADR-0006 description/alias warning emission carry-forward, plus the Electron GUI + parallel `build-gui.yml` workflow.
