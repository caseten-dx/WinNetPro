# Evidence — profile-save.feature

Feature: Save a profile

Demo: 002-save-and-dry-run-gui
Provider: fake
Clock (deterministic for evidence): 2026-05-19T12:00:00.000Z
Generated by: tsx --test tests/bdd/profile-save.test.ts

Source feature file: [docs/bdd/profile-save.feature](../../../docs/bdd/profile-save.feature)

## Scenario: Save the current state of an adapter as a new profile with preferred-adapter binding

**Status:** PASS

### Steps
- Given the fake provider is loaded from `./fixtures/adapters.json` — fixture written to scenario temp dir
- And the working directory is `./demo-config` (no profiles.json yet) — confirmed: `profiles.json` does not exist before run
- And the fixture defines an adapter "ad-02" with windowsAlias "Ethernet 5", macAddress "00-11-22-33-44-55", interfaceGuid "{a1b2c3d4-...-345678}", description "USB 10/100/1000 LAN", ipv4.mode static, ipv4.address 192.168.132.10, ipv4.prefixLength 24, ipv4.gateway null — set up via scenario env
- When the user runs `profiles save --name "DOCK 132" --from-adapter ad-02 --json`
- Then the exit code is 0 — got 0
- And the file "./demo-config/profiles.json" exists — true
- And profiles.json contains a profile with name "DOCK 132" — true
- And the saved profile's ipv4.address is "192.168.132.10" — got "192.168.132.10"
- And the saved profile's ipv4.prefixLength is 24 — got 24
- And the saved profile's ipv4.gateway is null — got null
- And the saved profile's preferredAdapter.macAddress is "00-11-22-33-44-55" — got "00-11-22-33-44-55"
- And the saved profile's preferredAdapter.interfaceGuid is "{a1b2c3d4-...-345678}" — got "{a1b2c3d4-...-345678}"
- And the saved profile's ipv6.mode is "inherit" — got "inherit"

### Exit code
```
0
```

### Stdout
```
{
  "id": "profile-dock-132",
  "name": "DOCK 132",
  "createdAt": "2026-05-19T12:00:00.000Z",
  "updatedAt": "2026-05-19T12:00:00.000Z",
  "preferredAdapter": {
    "macAddress": "00-11-22-33-44-55",
    "interfaceGuid": "{a1b2c3d4-...-345678}",
    "interfaceDescription": "USB 10/100/1000 LAN",
    "windowsAliasAtSaveTime": "Ethernet 5"
  },
  "ipv4": {
    "mode": "static",
    "address": "192.168.132.10",
    "prefixLength": 24,
    "gateway": null,
    "dns": {
      "mode": "inherit",
      "servers": []
    }
  },
  "ipv6": {
    "mode": "inherit"
  },
  "rollback": {
    "enabled": true
  }
}
```

### File: profiles.json
```json
[
  {
    "id": "profile-dock-132",
    "name": "DOCK 132",
    "createdAt": "2026-05-19T12:00:00.000Z",
    "updatedAt": "2026-05-19T12:00:00.000Z",
    "preferredAdapter": {
      "macAddress": "00-11-22-33-44-55",
      "interfaceGuid": "{a1b2c3d4-...-345678}",
      "interfaceDescription": "USB 10/100/1000 LAN",
      "windowsAliasAtSaveTime": "Ethernet 5"
    },
    "ipv4": {
      "mode": "static",
      "address": "192.168.132.10",
      "prefixLength": 24,
      "gateway": null,
      "dns": {
        "mode": "inherit",
        "servers": []
      }
    },
    "ipv6": {
      "mode": "inherit"
    },
    "rollback": {
      "enabled": true
    }
  }
]
```

## Scenario: Save a global profile (no preferred adapter)

**Status:** PASS

### Steps
- When the user runs `profiles save --name "Office DHCP" --ipv4-mode dhcp --global --json`
- Then the exit code is 0 — got 0
- And profiles.json contains a profile with name "Office DHCP" — true
- And the saved profile's ipv4.mode is "dhcp" — got "dhcp"
- And the saved profile does not have a preferredAdapter field — true

### Exit code
```
0
```

### Stdout
```
{
  "id": "profile-office-dhcp",
  "name": "Office DHCP",
  "createdAt": "2026-05-19T12:00:00.000Z",
  "updatedAt": "2026-05-19T12:00:00.000Z",
  "ipv4": {
    "mode": "dhcp",
    "address": null,
    "prefixLength": null,
    "gateway": null,
    "dns": {
      "mode": "inherit",
      "servers": []
    }
  },
  "ipv6": {
    "mode": "inherit"
  },
  "rollback": {
    "enabled": true
  }
}
```

### File: profiles.json
```json
[
  {
    "id": "profile-office-dhcp",
    "name": "Office DHCP",
    "createdAt": "2026-05-19T12:00:00.000Z",
    "updatedAt": "2026-05-19T12:00:00.000Z",
    "ipv4": {
      "mode": "dhcp",
      "address": null,
      "prefixLength": null,
      "gateway": null,
      "dns": {
        "mode": "inherit",
        "servers": []
      }
    },
    "ipv6": {
      "mode": "inherit"
    },
    "rollback": {
      "enabled": true
    }
  }
]
```

## Scenario: Reject saving with invalid IPv4 address

**Status:** PASS

### Steps
- When the user runs `profiles save --name "Bad" --ipv4-mode static --ipv4-address 999.0.0.1 --ipv4-prefix 24`
- Then the exit code is 3 — got 3
- And stderr contains "invalid IPv4 address" — got: "invalid IPv4 address: 999.0.0.1"
- And profiles.json is not created — exists: false

### Exit code
```
3
```

### Stderr
```
invalid IPv4 address: 999.0.0.1
```

### Notes
- profiles.json was never created — confirmed via existsSync.

## Scenario: Reject saving with conflicting flags (dhcp mode + static address)

**Status:** PASS

### Steps
- When the user runs `profiles save --name "Bad" --ipv4-mode dhcp --ipv4-address 192.168.1.1 --ipv4-prefix 24`
- Then the exit code is 3 — got 3
- And stderr contains "dhcp mode does not allow ipv4 address" — got: "dhcp mode does not allow ipv4 address\ndhcp mode does not allow ipv4 prefix"

### Exit code
```
3
```

### Stderr
```
dhcp mode does not allow ipv4 address
dhcp mode does not allow ipv4 prefix
```
